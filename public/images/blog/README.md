# Imagens do Blog

Use esta pasta para capas e imagens internas dos artigos.

Padrão sugerido:

`slug-do-artigo-capa.jpg`

As primeiras publicações reutilizam fotografias já aprovadas de outras seções. Quando houver imagens editoriais próprias, atualize o campo `image` do artigo em `content/blog.ts`.

